- title t('.title')

- content_for :actionbar do
  = link_to t('.new_bestellrunde'), new_admin_bestellrunde_path, class: 'btn btn-primary'

.well.well-small
  = form_tag admin_bestellrunden_path, :method => :get, :remote => true,
    'data-submit-onchange' => true, class: 'form-search' do
    = text_field_tag :query, params[:query], class: 'input-medium search-query',
      placeholder: t('admin.search_placeholder')
#bestellrunden
  = render "bestellrunden"